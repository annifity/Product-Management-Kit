{
  "name": "authorized accepted-value change requires confirmation",
  "request": "authorized-change.request.json",
  "expectedState": "confirmation-required",
  "expectedValues": {
    "story.boundary": "single-end-to-end-story"
  },
  "expectedProvenance": {
    "story.boundary": "active-request"
  },
  "requiredConflictKinds": [
    "authorized-accepted-value-change"
  ],
  "requiredConflictSeverities": [
    "warning"
  ]
}

