# Stable Delivery Scope

Accepted spec `OPS-SPEC-011@1.0` contains two confirmed capabilities: view
unassigned approved requests and assign one owner. The assignment view depends
on the approved-request query. Prepare release slices, milestones, dependencies,
and grooming questions without adding behavior.
