# Confirmed Direction

The problem is that regional operations leads cannot see which approved service
requests still lack an owner. Target users are regional operations leads. The
desired outcome is faster assignment of approved requests without changing the
assignment workflow. Success is measured by median time from approval to first
owner assignment. Produce a compact direction brief; implementation detail is
not requested.
