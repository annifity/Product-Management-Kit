# Input

Turn an accepted, versioned product specification into a traceable UX/UI
design handoff. Preserve requirement, permission, state, accessibility, and
responsive constraints without inventing product behavior.

The result may include a self-contained local HTML preview, but it must remain
a reviewable design artifact rather than production frontend code.
