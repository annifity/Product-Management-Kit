# Cursor Adapter Notes

Cursor receives Annifity behavior through `.cursor/rules/annifity-*.mdc`.

- `annifity-index.mdc` is always applied.
- Per-skill rules should point back to canonical `skills/<name>/SKILL.md`.
- Do not edit generated Cursor rules manually.
